# .github/workflows/generate-pdf.yml
name: Generate Final Document PDF

on:
  push:
    branches:
      - main
      - develop
      - ci/pdf-automation
  pull_request:
    branches:
      - main
      - develop
      - ci/pdf-automation

jobs:
  build-pdf:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Compile unified Markdown document
        run: bash docs/scripts/merge.sh

      - name: Inject dynamic Table of Contents
        run: node docs/scripts/generate-toc.js

      - name: Clean document text (Regex/Sed)
        run: sed -i 's/\.md://g' full_document.md

      - name: Generate Main PDF
        run: npx md-to-pdf full_document.md --config-file md-to-pdf.config.json

      - name: Rename Main PDF to final filename
        run: mv full_document.pdf documentacion-final.pdf

      - name: Generate Changelog PDF
        run: npx md-to-pdf changelog.md --config-file md-to-pdf.config.json

      - name: Rename Changelog PDF to final filename
        run: mv changelog.pdf registro-de-cambios.pdf

      - name: Upload PDF Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: documentacion-final
          path: |
            documentacion-final.pdf
            registro-de-cambios.pdf
          retention-days: 14
